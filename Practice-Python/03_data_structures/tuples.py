#Tuples are similar to lists, but we can't modify it

number = (1, 2, 3)
number.append(2) #shows error since we cant modify it

print(number.count(1)) #counts how many times 1 has repeated, since it provides information not modifying it